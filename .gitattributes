# Normalize line endings: LF in the repo, native on checkout. Keeps Windows
# working copies and the Linux CI runner in agreement so `ruff format --check`
# stays deterministic across platforms.
* text=auto eol=lf
