# ⚠️ Synthetic results

The results in this directory are **synthetic placeholders** generated by `scripts/generate_synthetic_data.py`, not real measurements. They exist so the dashboard and notebook can be built and browsed before a real corpus and API runs exist. Every manifest records `guideline_gpt_version: SYNTHETIC-DEMO`.

Re-run with real experiments via `eval-harness run` once a corpus is ingested.
