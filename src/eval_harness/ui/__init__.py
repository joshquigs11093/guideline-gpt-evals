"""Streamlit dashboard for browsing pre-computed evaluation results.

The dashboard reads ``results/*/*/metrics.json`` only — no live experiments and
no API keys required. Launch with ``streamlit run src/eval_harness/ui/dashboard.py``.
"""
